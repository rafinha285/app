[package]
name = "backend"
version = "0.1.0"
edition = "2021"

[dependencies]
hyper = { version = "0.14", features = ["full", "server"] }
tokio = { version = "1", features = ["full"] }
routerify = "3.0"
